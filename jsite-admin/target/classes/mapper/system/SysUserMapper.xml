<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsite.system.mapper.SysUserMapper">

    <resultMap type="com.jsite.system.domain.SysUser" id="SysUserResult">
        <id property="userId" column="id"/>
        <result property="deptId" column="dept_id"/>
        <result property="loginName" column="login_name"/>
        <result property="userName" column="user_name"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phone"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_time"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <association property="dept" javaType="com.jsite.system.domain.SysDept" resultMap="deptResult"/>
    </resultMap>

    <resultMap id="deptResult" type="com.jsite.system.domain.SysDept">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leader" column="leader"/>
        <result property="status" column="dept_status"/>
    </resultMap>

    <sql id="selectUserVo">
        select u.id, u.dept_id, u.login_name, u.user_name, u.email, u.avatar, u.phone, u.password, u.salt, u.sex, u.status, u.del_flag, u.login_ip, u.login_time, u.create_by, u.create_time, u.remark,
               d.id as dept_id, d.parent_id, d.ancestors, d.dept_name, d.order_num, d.leader, d.status as dept_status
        from sys_user u
        left join sys_dept d on u.dept_id = d.id
    </sql>

    <select id="selectUserList" resultMap="SysUserResult">
        select u.id, u.dept_id, u.login_name, u.user_name, u.email, u.avatar, u.phone, u.sex, u.status, u.del_flag, u.login_ip, u.login_time, u.create_by, u.create_time, u.remark, d.dept_name, d.leader
        from sys_user u
        left join sys_dept d on u.dept_id = d.id
        where u.del_flag = '0'
        <if test="user.userId != null and user.userId != 0">
            AND u.id = #{user.userId}
        </if>
        <if test="user.userName != null and user.userName != ''">
            AND u.user_name like concat('%', #{user.userName}, '%')
        </if>
        <if test="user.loginName != null and user.loginName != ''">
            AND u.login_name like concat('%', #{user.loginName}, '%')
        </if>
        <if test="user.status != null and user.status != ''">
            AND u.status = #{user.status}
        </if>
        <if test="user.phonenumber != null and user.phonenumber != ''">
            AND u.phone like concat('%', #{user.phonenumber}, '%')
        </if>
        <if test="user.deptId != null and user.deptId != 0">
            AND (u.dept_id = #{user.deptId} OR u.dept_id IN ( SELECT t.id FROM sys_dept t WHERE find_in_set(#{user.deptId}, ancestors) ))
        </if>
        order by u.create_time desc
    </select>

    <select id="selectUserByUserName" parameterType="String" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where u.login_name = #{userName} and u.del_flag = '0'
    </select>

    <select id="selectUserById" parameterType="Long" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where u.id = #{userId}
    </select>

    <select id="countUserByDeptId" resultType="Integer">
        select count(*) from sys_user where dept_id = #{deptId} and del_flag = '0'
    </select>

    <select id="checkUserNameUnique" parameterType="String" resultMap="SysUserResult">
        select id, login_name from sys_user where login_name = #{userName} and del_flag = '0' limit 1
    </select>

    <select id="checkPhoneUnique" parameterType="String" resultMap="SysUserResult">
        select id, phone from sys_user where phone = #{phonenumber} and del_flag = '0' limit 1
    </select>

    <select id="checkEmailUnique" parameterType="String" resultMap="SysUserResult">
        select id, email from sys_user where email = #{email} and del_flag = '0' limit 1
    </select>

</mapper>
